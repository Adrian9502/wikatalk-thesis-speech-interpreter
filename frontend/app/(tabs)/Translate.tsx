import { StyleSheet, Text, SafeAreaView } from "react-native";
import React from "react";

const Translate = () => {
  return (
    <SafeAreaView className="flex-1 bg-emerald-500 items-center justify-center">
      <Text className="text-lg font-bold">Translate</Text>
    </SafeAreaView>
  );
};

export default Translate;

const styles = StyleSheet.create({});
