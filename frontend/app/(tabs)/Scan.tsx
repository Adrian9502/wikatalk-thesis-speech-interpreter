import { StyleSheet, Text, View, SafeAreaView } from "react-native";
import React from "react";

const Scan = () => {
  return (
    <SafeAreaView className="flex-1 bg-emerald-500 items-center justify-center">
      <Text className="text-lg font-bold">Scan</Text>
    </SafeAreaView>
  );
};

export default Scan;

const styles = StyleSheet.create({});
